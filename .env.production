# Production runtime env vars su PRAZNI ovde — popunjavaju se kroz
# Docker container environment (window._env_ injection iz /config.js).
# Ovo polje sluzi samo da Vite ne ugradi dev VITE_API_URL u prod build.
VITE_API_URL=
VITE_OUR_BANK_CODE=RN-222
VITE_ENV=production
