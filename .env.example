# Frontend runtime env vars (citaju se kroz window._env_ pri container startup-u).
# Za local dev (npm run dev) Vite cita ove iste preko import.meta.env (fallback path).
VITE_API_URL=http://localhost:8080
VITE_OUR_BANK_CODE=RN-222
VITE_ENV=development
